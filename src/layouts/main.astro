---
import "./globals.css"
const { content } = Astro.props;
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>{content.title}</title>
	</head>
	<body>
		<div class="fixed top-0 left-0 right-0 h-12 bg-gray-800 text-white flex items-center justify-between px-4 z-10">
			<span>ロゴ</span>
			<span>Login</span>
		</div>
		<article class="prose max-w-[80ch] m-auto mt-16">
			<slot />
		</article>
	</body>
</html>
